# SAST for the TS surface (#39). Rust static analysis runs as cargo clippy
# (-D warnings) + cargo audit inside ci.yml's anchor-tests job.
#
# NOTE: this repo is PRIVATE without GitHub Advanced Security, so SARIF upload
# to the code-scanning UI is unavailable (422). We run the same analysis and
# gate LOCALLY instead: any error-level CodeQL finding fails the job; the full
# SARIF is kept as a build artifact for review.
name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '17 4 * * 1' # weekly sweep so new queries cover old code

jobs:
  analyze:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read # the analyze action reads its own workflow-run metadata

    steps:
      - uses: actions/checkout@v4

      - uses: github/codeql-action/init@v3
        with:
          languages: javascript-typescript
          queries: security-extended

      - uses: github/codeql-action/analyze@v3
        with:
          upload: never
          output: sarif-results

      - name: Gate on error-level findings (no GHAS upload available)
        run: |
          SARIF=$(ls sarif-results/*.sarif | head -1)
          echo "Gating on: $SARIF"
          ERRORS=$(jq '[.runs[].results[]? | select(.level == "error")] | length' "$SARIF")
          TOTAL=$(jq '[.runs[].results[]?] | length' "$SARIF")
          echo "CodeQL findings: $TOTAL total, $ERRORS error-level"
          jq -r '.runs[].results[]? | "\(.level)\t\(.ruleId)\t\(.locations[0].physicalLocation.artifactLocation.uri):\(.locations[0].physicalLocation.region.startLine)"' "$SARIF" || true
          test "$ERRORS" -eq 0

      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: codeql-sarif
          path: sarif-results
          retention-days: 30
